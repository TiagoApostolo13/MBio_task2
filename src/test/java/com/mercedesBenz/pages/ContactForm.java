package com.mercedesBenz.pages;

import com.mercedesBenz.NavigationUtil;
import org.openqa.selenium.WebDriver;

public class ContactForm extends NavigationUtil {
    public ContactForm(WebDriver driver) {
        super(driver);
    }
}
